{
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build": "tsc --build packages/types packages/core packages/server packages/react",
    "check": "biome check packages/*/src/",
    "prepare": "lefthook install",
    "test": "vitest run",
    "docs:dev": "pnpm --filter @neeter/docs dev",
    "docs:build": "pnpm --filter @neeter/docs build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "jsdom": "^26.0.0",
    "lefthook": "^2.1.1",
    "typescript": "~5.9.3",
    "vitest": "^4.0.18"
  }
}
